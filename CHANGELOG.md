# 更新履歴

## v1.10.0

- マスタ管理をモーダルから独立した画面（マスタ管理画面）に変更しました。ダッシュボードの「マスタ管理」ボタンを押すと画面が切り替わり、「← ダッシュボードに戻る」で戻れます
- マスタ管理画面を「部屋」「スタッフ」のタブに分け、それぞれ別々に一覧・追加ができるようにしました
- 並び替えを▲▼ボタンからドラッグ&ドロップ方式に変更しました。項目左側のハンドル（⠿）をつかんで上下に動かすと並び順を変更できます

## v1.9.0

- マスタ管理（部屋・スタッフ）に「編集」ボタンを追加し、名前を後から変更できるようにしました
- マスタ管理の各項目に「▲」「▼」ボタンを追加し、表示順を自由に並び替えられるようにしました
- ここで設定した並び順は、ダッシュボードの部屋カードや、入室登録時の部屋・スタッフの選択肢の表示順にもそのまま反映されます

## v1.8.4

- 時計横のファビコンを28px→40pxに拡大しました
- 更新履歴モーダルの見出しにもファビコンを表示するようにしました

## v1.8.3

- 画面左上の時計の隣にファビコン（ドアと犬のアイコン）を表示するようにしました
- 時計の表示を「時:分」から「時:分:秒」に変更しました

## v1.8.2

- 自動アップデートの不具合を修正しました：新しく追加されたファイル（CHANGELOG.mdなど）が、
  それを追加した回の更新では同期対象一覧に反映されず、以後ずっと同期されなくなる場合がありました
- 同期対象ファイルの一覧を、更新先のコミット自身が持つ`sync-manifest.json`から都度取得するように変更し、
  今後この種の取りこぼしが起きないようにしました（取得に失敗した場合は従来通りの内蔵一覧にフォールバック）
- 店舗のPCで更新履歴が読み込めなかった場合、この更新の適用後に再起動すると解消されます

## v1.8.1

- 起動時にChromeをキオスクモード（全画面）で開いていたのをやめ、既定のブラウザで通常のウィンドウとして開くように変更しました
- ※この変更はrun-app.batに対するものなので自動アップデートの対象外です。反映するには新しいセットアップキットでinstall.batを再実行してください

## v1.8.0

- 「1コールまで」のアラームを止めるまでは「退室まで」の残り時間が進まないように変更しました
- 止めた時点を起点に、退室フェーズ本来の長さ分だけ改めてカウントを開始します（気付かずに放置していた時間の分、お客様の退室時刻が不利にならないようにするため）
- 部屋カードの色（まもなく終了／時間超過）や進捗バーもこの仕様に合わせて連動するようにしました

## v1.7.2

- 退室ボタンの確認待ち表示（「本当に退出させますか？」）を、文言変更だけでなく背景色をオレンジ色に変え、拡大縮小のアニメーションで点滅させるように変更し、気付きやすくしました

## v1.7.1

- 退室ボタンの2段階確認で、一定時間（4秒）操作がないと確認状態が自動的に解除される仕様になっていたため、
  1回目を押してから間を置いて2回目を押すと退室できない不具合を修正しました
- 自動解除の仕組みを廃止し、実際に退室処理を行うか、時間調整など他の操作をするまで確認状態を保持するように変更しました

## v1.7.0

- 退室ボタンを2段階確認に変更しました（1回目で「本当に退出させますか？」の表示に変わり、2回目を押すと退室処理を実行。ブラウザの標準アラートは使用しません）
- ファビコンを追加しました（ドアと犬のイラスト）

## v1.6.0

- 画面上で現在のバージョンと更新履歴を確認できるようにしました

## v1.5.0

- GitHubリポジトリと連携し、起動時に自動でアプリ本体を最新化する機能を追加しました
- インターネットに繋がらない場合や確認に失敗した場合は、既存のファイルでそのまま起動します

## v1.4.0

- 「1コール」「退室」の通知音を個別に設定できるようにしました
- 通知音に効果音ファイル（成功音・警告音1・ルーレット点滅音）を追加しました

## v1.3.0

- 入室登録後は「待機中」状態になり、「▶ スタート」ボタンを押した時点から時間計測を開始するように変更しました
- 待機中の登録を取り消せる「取消」ボタンを追加しました
- スタート待ちの間に清掃できるようになったため、清掃工程を廃止し、退室後は直接空室に戻るようにしました

## v1.2.0

- 担当スタッフを「未選択」のまま入室登録できるようにしました

## v1.1.0

- 通知音の種類を4種類から選べる設定を追加しました（試聴機能付き）
- 部屋ごとの前回担当スタッフを、入室登録時に自動選択するようにしました
- 空室カードに前回担当者を表示するようにしました
- 利用時間の初期値を設定画面で変更できるようにしました

## v1.0.0

- 初回リリース
- 部屋の空室管理ダッシュボード（一覧表示・色分け・ダークモード対応）
- 入室登録・退室処理・清掃完了の基本フロー
- 「1コール」「退室」の2段階タイマーアラーム（音声通知・停止ボタン・時間調整）
- 履歴のCSV出力
- 部屋・スタッフのマスタ管理
- Windows用ワンクリックインストーラー
